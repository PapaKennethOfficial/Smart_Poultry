node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Ignore generated uploads
/uploads

# Firebase service-account private key — must never be committed
firebase-adminsdk.json
*-firebase-adminsdk-*.json

